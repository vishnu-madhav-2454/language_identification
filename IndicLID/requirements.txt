# ==============================================================
# IndicLID Requirements
# Paper: "Bhasha-Abhijnaanam: Native-script and Romanized
#         Language Identification for 22 Indic Languages"
#         ACL 2023 — AI4Bharat
#
# Install with: pip install -r requirements.txt
# ==============================================================

# ── Core ML / DL ──────────────────────────────────────────────
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.24.0
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
evaluate>=0.4.0

# ── FastText (Linear Classifier) ──────────────────────────────
# On Windows, use fasttext-wheel instead of fasttext
fasttext-wheel; sys_platform == "win32"
fasttext; sys_platform != "win32"

# ── HuggingFace utilities ──────────────────────────────────────
huggingface_hub>=0.17.0
tokenizers>=0.14.0
sentencepiece>=0.1.99    # required by IndicBERT tokenizer

# ── Transliteration (AI4Bharat IndicXlit) ─────────────────────
ai4bharat-transliteration

# ── Data download & handling ───────────────────────────────────
requests>=2.31.0
tqdm>=4.66.0

# ── Evaluation & Visualization ────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.0
